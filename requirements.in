# enc-frx runtime dependencies.
#
# Pulled from the Fractalyze package index alongside upstream PyPI:
#   pip install -r requirements.in \
#     --extra-index-url https://fractalyze.github.io/pypi/simple/
#
# hash-frx — the symmetric layer ML-KEM's sampling and hashing run through — is
# absent on purpose: it is a Bazel module, pinned by commit in //MODULE.bazel,
# not a wheel.
#
# The frx build is pinned to the one hash-frx pins, and the two must move
# together: hash-frx resolves frx from its own lock, so skew puts two different
# frx copies on one test's sys.path.
#
# Which frx build that is, is hash-frx's `frx>=` floor to choose, and it is not
# read off a feature landing. On GPU — and only there, the arms being GPU-only —
# Keccak lowers through hash-frx's dedicated emitters, and a wheel that merely
# *carries* an emitter is not a wheel where it is correct: the first one to carry
# these miscompiled the fused permutation. The two failure modes sit on either
# side of the floor and neither is visible from here — below it the marker is
# emitted but not recognized, which gives right bytes and no kernels on any
# backend, and on the wheel where the emitter first appeared it gives wrong ones
# on the only backend that runs it. Follow the floor, not the changelog.
#
# zk_dtypes arrives via frx but is pinned here directly, because this repo has a
# floor of its own on it. AES and GCM are defined over binary fields this stack
# carries natively — `binary_field_gf8_aes` (GF(2^8), AES's reduction polynomial)
# and `binary_field_ghash` (GF(2^128), GCM's) — and those dtypes register in the
# frx frontend from zk_dtypes >= 0.0.10. Taken transitively, a frx bump could
# drop the thing AES-GCM is built on without anything here saying so.
#
# The floor is 0.0.13 for a second reason since ML-KEM: `zk_dtypes.prime_field`,
# the factory that mints a field from a runtime modulus, does not exist before
# it. ML-KEM's q = 3329 is not one of the curated families, so the NTT has no
# dtype to run on without it — the failure is an AttributeError at import, not a
# wrong result. Held at 0.0.14, the release this repo has been tested against.
absl-py
numpy
frx==0.10.2.dev20260818233737
frxlib==0.10.2.dev20260818233737
frx-cuda12-pjrt==0.10.2.dev20260818233737
frx-cuda12-plugin==0.10.2.dev20260818233737
zk-dtypes==0.0.14
